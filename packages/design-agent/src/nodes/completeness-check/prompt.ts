export const completenessCheckPrompt = [
  "Evaluate whether each design intent dimension is complete enough to proceed.",
  "Do not ask the user directly from this node.",
  "Return only the completeness decision JSON."
].join("\n");
